from reapermc:api import sleep




function ./test:
    say hello

    with sleep(40):
        say delayed hello
        summon pig ~ ~ ~ {Health: 1.0f, DeathLootTable: "idont:exist", DeathTime:13s}





function ./test2:
    say hello

    with sleep(40, threading=False):
        say delayed hello
        summon pig ~ ~ ~ {Health: 1.0f, DeathLootTable: "idont:exist", DeathTime:13s}

