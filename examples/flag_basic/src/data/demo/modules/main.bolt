from reapermc:api import Flag, is_in_air, is_sneaking



function ./test_in_air:
    if Flag(is_in_air):
        say 'in air!'
    else:
        say 'on ground!'


function ./test_sneaking:
    if Flag(is_sneaking):
        say 'true'
    else:
        say 'false'



